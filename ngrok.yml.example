# ngrok configuration file
# Copy this to ngrok.yml and add your authtoken

version: "2"
authtoken: YOUR_NGROK_AUTHTOKEN_HERE

tunnels:
  backend:
    addr: backend:4000
    proto: http
    inspect: true

